# PKS_Sem_1 — Учебные проекты 1-го семестра (ПКС)

Репозиторий содержит несколько учебных проектов, выполненных в рамках курса **Программирование корпоративных систем (ПКС)** за **1-й семестр**. Каждый проект — отдельная задача, реализованная на C#.

---

## 📁 Структура репозитория

| Папка / проект            | Описание проекта                                                                                             | Основной файл / запуск            |
|---------------------------|--------------------------------------------------------------------------------------------------------------|-----------------------------------|
| `Calculator_PR1_PKS`      | Классический калькулятор: `+`, `−`, `*`, `/`, `% (процент)`, `1/x`, `x²`, `√x` и память (`M+`, `M−`, `MR`). | `Program.cs`, `Calculator_PR1_PKS.csproj` |
| `PR_1_2_Calendar_and_ATM` | Два задания: **календарь мая** и **банкомат** (меню выбора внутри).                                         | `Program.cs`, проектная папка    |
| `KR_2_all`                | Сборник задач **Практика №2** и **Практика №2.2** — вариант 20. Меню внутри запускает любое задание.        | `Program.cs`, проектная папка    |
| `KR_3_4`                  | Сборник задач **Практика №3**, **Практика №3.2** и **Практика №4** (рекурсия, бронирование, заказы).        | `Program.cs`, проектная папка    |
| `Task_PR_13_09_2025`      | Задания с практики от 13.09.2025.                                                                           | `Program.cs`                     |

---

## Проект 1: Калькулятор (Calculator_PR1_PKS)

**Функционал:**
- Бинарные операции: `+`, `−`, `×`, `/`, остаток от деления.  
- Унарные операции: обратное число (`1/x`), квадрат (`x²`), корень (`√x`).  
- Работа с памятью: `M+` (добавить к памяти), `M−` (вычесть из памяти), `MR` (взять значение памяти).  
- Устойчивый ввод: поддержка десятичного разделителя и запятой/точки.  
- Меню для многократных операций.

**Ограничения:**
- Используется тип `double`, без поддержки большой точности.  
- Программа консольная (без GUI).  
- Память работает только в пределах одного запуска (не сохраняется на диск).

**Обработка ошибок:**
- Деление на ноль и `1/0` — исключения с сообщением.  
- Корень из отрицательного — сообщение об ошибке.  
- Неправильный ввод — повторный запрос.

---

## Проект 2: Календарь мая + Банкомат (PR_1_2_Calendar_and_ATM)

### Задание 1: Календарь мая

**Условие:**
- При вводе дня месяца проверяется, рабочий это день или выходной.  
- Возможность задать, с какого дня недели начинается май (1 — понедельник … 7 — воскресенье).  
- Выходные — все субботы и воскресенья, а также даты с **1 по 5** и **8 по 10 мая** включительно.

**Функционал:**
- Ввод начала месяца (день недели).  
- Ввод номера дня мая.  
- Определение рабочего/выходного дня.

**Ограничения:**
- Только май (31 день).  
- Корректность входных значений: день недели 1–7, день мая 1–31.

**Обработка ошибок:**
- Неправильный ввод числа — повтор запроса.  
- Ввод дня мая вне диапазона — сообщение и повтор.  
- Ввод дня недели вне диапазона — сообщение и повтор.

### Задание 2: Банкомат

**Условие:**
- Пользователь хочет снять сумму **N** рублей.  
- Купюры: 100, 200, 500, 1000, 2000, 5000 рублей.  
- Банкомат выдаёт **наименьшим количеством купюр** либо сообщает, что выдать сумму невозможно.  
- Максимальная сумма за раз — 150 000 рублей.

**Функционал:**
- Ввод суммы (с проверками).  
- Расчёт количества купюр по номиналам.  
- Вывод результата или сообщения об ошибке.

**Ограничения:**
- Сумма должна быть кратна 100.  
- Не превышать 150 000.  
- Используется “жадный” алгоритм (наибольшие купюры первыми).

**Обработка ошибок:**
- Ввод нецелого числа — повтор запроса.  
- Невозможность выдать сумму — сообщение.  
- Ввод вне диапазона — сообщение и запрос заново.

---

## Проект 3: Практика №2 и №2.2, вариант 20 (`KR_2_all`)

Этот проект объединяет **все задания практических работ №2 и №2.2** по дисциплине  
**«Программирование корпоративных систем» (вариант 20)**.  
Выполнен в виде консольного приложения с единым меню выбора задания.

### 🔢 Список заданий

1️⃣ **Калькулятор матриц**  
   - Создание двух матриц (ввод вручную или случайная генерация в диапазоне `[a; b]`).  
   - Основные операции:  
     - сложение,  
     - умножение,  
     - транспонирование,  
     - вычисление определителя (`det(A)`),  
     - нахождение обратной матрицы (`A⁻¹`).  
   - Решение системы линейных уравнений `Ax = b` с классификацией:  
     - ❌ *нет решений*,  
     - ♾️ *бесконечно много решений*,  
     - ✅ *единственное решение* (с выводом `x₁, x₂, …`).  
   - Проверка размерностей и корректная обработка ошибок (например, `det(A) = 0`).  

2️⃣ **Ряды (ряд Маклорена для `arcth(x)`)**  
   - Формула:  
     ```text
     arcth(x) = x + x³/3 + x⁵/5 + x⁷/7 + … , |x| < 1
     ```
   - Ввод `x` и точности `ε` (`0 < ε < 0.01`).  
   - Последовательное суммирование до условия `|член| < ε`.  
   - Вывод:
     - значение суммы,
     - количество членов ряда,
     - значение `n`-го члена.  

3️⃣ **Счастливый билет**  
   - Ввод **строки длиной ровно 6 символов**, состоящей из цифр.  
   - Без “обрезания” ведущих нулей (например, `001231` считается корректным).  
   - Проверка равенства суммы первых трёх и последних трёх цифр.  
   - Запрет некорректного ввода (меньше/больше 6 символов, нецифровые символы).  

4️⃣ **Сокращение дроби**  
   - Ввод `M/N`.  
   - Сокращение дроби до несократимого вида (по НОД).  
   - Обработка ошибок:
     - `N = 0` — сообщение о недопустимом знаменателе,  
     - отрицательный знаменатель — перенос знака в числитель.  

5️⃣ **Угадай число (0..63)**  
   - Компьютер за ≤6 шагов отгадывает число пользователя,  
     задавая вопросы вида «Ваше число больше X? (1/0)».  
   - Используется бинарный поиск.  

6️⃣ **Кофейный аппарат**  
   - Напитки:  
     - ☕ **Американо** — 300 мл воды, 150 ₽;  
     - 🥛 **Латте** — 30 мл воды + 270 мл молока, 170 ₽.  
   - В начале вводятся запасы воды и молока.  
   - Пользователь выбирает напитки до исчерпания ингредиентов или выхода (`q`).  
   - Если ингредиентов на напиток не хватает, этот вариант убирается из доступных.  
   - После завершения выводится отчёт:  
     - остатки воды и молока,  
     - количество приготовленных чашек каждого вида,  
     - общая выручка.  

7️⃣ **Лабораторный опыт (бактерии и антибиотик)**  
   - Начальное количество бактерий `N`.  
   - Каждую каплю антибиотика (`X`) описывает эффект:  
     - в 1-й час каждая капля убивает 10 бактерий,  
     - во 2-й — 9, далее на 1 меньше до 0.  
   - Каждый час бактерии **удваиваются**, затем применяется антибиотик.  
   - Процесс продолжается, пока есть активные капли и бактерии.  
   - На каждом шаге выводится количество живых бактерий.  

8️⃣ **Колонизация Марса**  
   - Есть `n` прямоугольных модулей с размерами `a × b`, которые нужно разместить в прямоугольном поле `w × h`.  
   - Модуль оборачивается защитным слоем толщиной `d` ⇒ фактический размер `(a + 2d) × (b + 2d)`.  
   - Требуется найти **максимальную толщину `d`**, при которой модули можно разместить (возможно, поворачивая их на 90°).  
   - Решение методом **бинарного поиска** по `d` с проверкой размещаемости.

---

### 📊 Примеры тестов для задачи «Матрицы»

| Матрица A                  | Вектор b   | Результат                          |
|----------------------------|-----------|------------------------------------|
| [[3, −4], [6, −8]]         | [15, 11]  | ❌ Нет решений                     |
| [[3, −4], [6, −8]]         | [15, 30]  | ♾️ Бесконечно много решений        |
| [[3, −4], [1, 1]]          | [13, 9]   | ✅ Единственное решение: x₁=7,x₂=2 |

---

## Проект 4: Практика №3, №3.2 и №4 (`KR_3_4`)

Единый консольный проект с меню:

1) Практика 3 — Методы. Рекурсия  
2) Практика 3.2 — Система бронирования столов  
3) Практика 4 — Система заказов  

### 4.1. Практика 3 — Методы. Рекурсия

В проекте реализованы задачи на **чистую рекурсию** без использования строк и циклов там, где это запрещено методичкой.

**Задание 1. Разворот числа без нулей**

- Вводится **целое положительное число без цифры `0` и без ведущих нулей**.  
- Валидация реализована **арифметически**, без перевода в строку.  
- Число разворачивается с помощью рекурсивной функции.  
- Пример:  
  - ввод: `52` → вывод: `25`;  
  - ввод `00025` или число, содержащее `0` → ввод отклоняется.

**Задание 2. Рекурсивная функция с контролем переполнения стека**

- Вторая задача (рекурсивный алгоритм по методичке) реализована с:  
  - ограничением глубины рекурсии,  
  - обработкой возможного переполнения стека через `try / catch`,  
  - сообщением пользователю, если рекурсия «ушла слишком глубоко» (например, некорректный набор параметров вроде `5, 1`).

### 4.2. Практика 3.2 — Система бронирования столов

Интерактивная система бронирования столов в ресторане.

**Параметры:**
- Время работы: **09:00–18:00** (9 часовых слотов).  
- Каждый стол имеет:  
  - `Id`,  
  - расположение (у окна, у прохода и т.п.),  
  - количество мест,  
  - расписание по часам.

**Меню системы:**
1. Добавить стол.  
2. Редактировать стол (по `ID`). Редактирование запрещено, если есть **активные бронирования**.  
3. Показать стол (по `ID`) — вывод полного расписания с пометкой слотов `свободно` / `занято`.  
4. Показать доступные столы по фильтру:  
   - минимальное количество мест,  
   - интервал времени (9–18),  
   - необязательный фильтр по расположению.  
5. Создать бронь:  
   - имя клиента, телефон, интервал времени, требуемое количество мест и комментарий,  
   - автоматический подбор свободных столов,  
   - выбор `ID` стола из списка подходящих.  
6. Изменить бронь (изменение клиента, телефона, времени, комментария с проверкой занятости стола).  
7. Отменить бронь (освобождаются слоты расписания).  
8. Показать все бронирования (состояние: активна/отменена).  
9. Найти бронь по имени и **последним 4 цифрам телефона** (без учёта регистра имени, строгая проверка “ровно 4 цифры”).  
0. Выход в главное меню.

Сделан упор на **валидацию ввода** (часы, ID, число мест, формат последних 4 цифр телефона).

### 4.3. Практика 4 — Система заказов

Модель небольшой ресторанной системы: меню блюд + заказы.

#### Структура:

- **Dish** — блюдо в меню:
  - `Id`, название, состав,  
  - **вес в формате `A/B/C`**,  
  - цена, категория (`Drinks`, `Salads`, `Soups`, `MainCourses`, …),  
  - время приготовления (минуты),  
  - список «типов» (острое, веганское, без глютена и т.п.).

- **Order** — заказ:
  - `Id`, номер столика, `WaiterId`, комментарий,  
  - время открытия и закрытия,  
  - список позиций (`OrderItem`: блюдо + количество),  
  - текущая сумма и признак закрыт/открыт.

- **Статистика** (`OrderStatistics`):
  - `CalcClosedTotal(in List<Order>, out decimal)` — сумма всех закрытых заказов,  
  - `CountClosedByWaiter(in List<Order>, int waiterId, out decimal total)` — статистика по официанту,  
  - `CollectDishStats(params Order[] orders)` — сколько раз заказывали каждое блюдо.

#### Формат веса `A/B/C`

Согласно методичке, вес блюда задаётся **строкой вида `100/20/50`**:

- `A` — основной компонент,  
- `B` — гарнир / доп. часть,  
- `C` — соус / жидкая часть.

Программа строго проверяет ввод:

- ровно **три числа**, разделённых двумя символами `/`;  
- только цифры, без пробелов;  
- каждое число > 0 и **без ведущих нулей** (`000/50/20` и `30/0/20` — некорректно).  

При ошибке формат поясняется и ввод повторяется.

#### Меню системы заказов

1. **Добавить блюдо**  
   - Вводятся: название, состав, вес `A/B/C`, цена (`0.01–100000`), время готовки, категория и список типовых меток.  
   - Все числовые поля валидируются, вес — по формату `A/B/C`.

2. **Редактировать блюдо**  
   - Выбор блюда по `Id`.  
   - Можно менять любое поле, оставив пустую строку — «без изменений».  

3. **Удалить блюдо**  
   - Удаление по `Id` с проверкой существования.

4. **Показать все блюда (меню)**  
   - Вывод меню, сгруппированного по категориям.

5. **Создать заказ**  
   - Ввод `Id` столика, `Id` официанта, комментария.  
   - Затем интерактивное добавление блюд (пока пользователь не введёт `0`).  

6. **Изменить заказ**  
   - Нельзя редактировать закрытый заказ.  
   - Можно:
     - добавить новые блюда;
     - изменить количество по уже добавленным блюдам (0 — удалить позицию).

7. **Закрыть заказ**  
   - Фиксирует время закрытия, пересчитывает итоговую сумму.

8. **Показать заказ**  
   - Краткая информация + список позиций и стоимость каждой.

9. **Печать чека**  
   - Чек только для закрытого заказа.  
   - Позиции сгруппированы по категориям; для каждой категории выводится подытог, затем общий итог.

10. **Стоимость всех закрытых заказов**  
    - Использует `CalcClosedTotal` с `in`/`out` параметрами.

11. **Статистика по официанту**  
    - Для выбранного `WaiterId` выводит количество закрытых заказов и суммарную выручку.

12. **Статистика по блюдам**  
    - Использует `CollectDishStats(params Order[] orders)`.  
    - Показывает, сколько раз каждый `Dish` был заказан.

0. **Назад в главное меню** проекта `KR_3_4`.

---

## Как запустить проекты

Обычно открывается решение `.sln` (**PKS_Sem_1.sln**) в Visual Studio / Rider и выбирается нужный проект как стартовый.

Примеры:

- Для калькулятора — запустить проект `Calculator_PR1_PKS`.  
- Для календаря/банкомата — запустить `PR_1_2_Calendar_and_ATM`.  
- Для практик №2 и №2.2 — запустить `KR_2_all`.  
- Для практик №3, 3.2 и 4 — запустить `KR_3_4`.

Через .NET CLI из корня репозитория:

```bash
dotnet build

dotnet run --project Calculator_PR1_PKS
dotnet run --project PR_1_2_Calendar_and_ATM
dotnet run --project KR_2_all
dotnet run --project KR_3_4
```

---

### 👤 Автор

**Петренко Екатерина Анатольевна**  
Группа: **ЭФБО-04-24**  
Семестр: **1 (Программирование корпоративных систем)**  
Профиль подготовки: **Фуллстек-разработка**  
Язык реализации: **C# (.NET 9.0)**  
